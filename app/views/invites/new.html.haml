%h1 New invite

- form_for(@invite) do |f|
  = f.error_messages
  %fieldset
    %ol
      %li
        = f.label :email, "Email:"
        = f.text_field :email
    = f.submit "Invite"

= link_to 'Back', invites_path